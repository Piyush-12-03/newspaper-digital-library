package com.newspaper.library.enums;

/**
 * Application-level error codes for API responses.
 * These codes should be stable and machine-readable.
 */
public enum ApiErrorCode {
  INVALID_REQUEST,
  INVALID_DATE_RANGE,
  DATE_RANGE_TOO_LARGE
}
