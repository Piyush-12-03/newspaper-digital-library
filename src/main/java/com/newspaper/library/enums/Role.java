package com.newspaper.library.enums;

/**
 * Enum representing user roles in the system.
 * - USER: Regular user with read-only access
 * - ADMIN: Administrator with full access including upload and user management
 */
public enum Role {
  USER,
  ADMIN
}
